// MadLibs in JS

function madlib(name, subject) {
    // (test output) console.log(`${name}'s favorite subject in school is ${subject}.`)
    return `${name}'s favorite subject in school is ${subject}.`;
  }

// edit input here
madlib('Felipe', 'Math');
